{
  "name": "carpooling",
  "private": true,
  "module": "index.ts",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "lint": "bun -F carpooling-frontend lint && bun -F carpooling-backend lint",
    "precommit": "lint-staged && bun -F carpooling-frontend precommit && bun -F carpooling-backend precommit",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "sort-package-json": "^2.14.0"
  }
}
