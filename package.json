{
  "name": "carpooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "lint": "bun lint:root && bun -F '*' --elide-lines 0 lint",
    "lint:fix": "bun lint:root:fix && bun -F '*' --elide-lines 0 lint:fix",
    "lint:root": "eslint .",
    "lint:root:fix": "eslint --fix ."
  },
  "dependencies": {
    "connect-mongo": "^5.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "eslint-plugin-package-json": "^0.26.0",
    "typescript": "^5.7.3"
  }
}
